import { NgThemeDirective } from './ng-theme.directive';

describe('NgThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgThemeDirective();
    expect(directive).toBeTruthy();
  });
});
